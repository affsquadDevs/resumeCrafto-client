# 📝 Як отримати значення для .env файлу

## 1. DATABASE_URL (MongoDB Connection String)

### Варіант A: Якщо у вас вже є MongoDB Atlas

1. Увійдіть на https://www.mongodb.com/cloud/atlas
2. Перейдіть в ваш кластер
3. Натисніть кнопку **"Connect"**
4. Виберіть **"Connect your application"**
5. Скопіюйте connection string (виглядає як `mongodb+srv://...`)
6. Замініть `<password>` на ваш пароль користувача бази даних
7. Додайте в кінець `?retryWrites=true&w=majority`

**Приклад:**
```
mongodb+srv://myuser:mypassword123@cluster0.xxxxx.mongodb.net/resume-craftor?retryWrites=true&w=majority
```

### Варіант B: Якщо у вас немає MongoDB Atlas (створити новий)

1. Перейдіть на https://www.mongodb.com/cloud/atlas/register
2. Зареєструйтеся (безкоштовно)
3. Створіть новий кластер (безкоштовний tier M0)
4. Створіть користувача бази даних:
   - Перейдіть в **Database Access** → **Add New Database User**
   - Введіть username та password (збережіть їх!)
   - Надайте права **Atlas admin** або **Read and write to any database**
5. Налаштуйте Network Access:
   - Перейдіть в **Network Access** → **Add IP Address**
   - Для розробки виберіть **"Allow Access from Anywhere"** (0.0.0.0/0)
   - Або додайте ваш поточний IP
6. Отримайте Connection String:
   - Перейдіть в **Clusters** → натисніть **"Connect"**
   - Виберіть **"Connect your application"**
   - Скопіюйте connection string
   - Замініть `<password>` на ваш пароль
   - Замініть `<dbname>` на `resume-craftor` (або залиште як є)
   - Додайте в кінець `?retryWrites=true&w=majority`

### Варіант C: Локальний MongoDB (якщо встановлений локально)

```env
DATABASE_URL="mongodb://localhost:27017/resume-craftor"
```

---

## 2. NEXTAUTH_SECRET (Секретний ключ для NextAuth)

Це випадковий рядок мінімум 32 символи. Є кілька способів його отримати:

### Варіант A: Генерація через термінал (рекомендовано)

```bash
openssl rand -base64 32
```

Скопіюйте результат і використайте його як `NEXTAUTH_SECRET`.

### Варіант B: Онлайн генератор

Перейдіть на https://generate-secret.vercel.app/32 і скопіюйте згенерований ключ.

### Варіант C: Node.js команда

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### Варіант D: Вручну (не рекомендується для production)

Можна використати будь-який довгий випадковий рядок, наприклад:
```
my-super-secret-key-for-nextauth-minimum-32-characters-long
```

**Важливо:** Для production використовуйте безпечний випадковий ключ (варіанти A, B або C).

---

## 3. NEXTAUTH_URL (URL вашого додатку)

### Для локальної розробки:

```env
NEXTAUTH_URL="http://localhost:3000"
```

### Для production (після деплою на Vercel):

```env
NEXTAUTH_URL="https://ваш-проект.vercel.app"
```

Vercel автоматично надасть вам URL після деплою (наприклад: `https://resume-craftor.vercel.app`).

---

## 📄 Приклад повного .env файлу

Створіть файл `.env` в папці `resumeCrafto-client`:

```env
# MongoDB Connection String
DATABASE_URL="mongodb+srv://myuser:mypassword123@cluster0.xxxxx.mongodb.net/resume-craftor?retryWrites=true&w=majority"

# NextAuth Secret (згенеруйте через: openssl rand -base64 32)
NEXTAUTH_SECRET="xK9mP2vQ7wR4tY8uI0oA3sD6fG1hJ5kL9nM2bV8cX1zA="

# NextAuth URL (для локальної розробки)
NEXTAUTH_URL="http://localhost:3000"
```

---

## ✅ Швидка перевірка

Після створення `.env` файлу, перевірте налаштування:

```bash
cd resumeCrafto-client
node check-setup.js
```

---

## 🔒 Безпека

**НЕ діліться:**
- Вашим `.env` файлом
- `DATABASE_URL` з паролем
- `NEXTAUTH_SECRET`

**Додайте `.env` в `.gitignore`** (якщо ще не додано), щоб не завантажити секрети в Git.

---

## 🆘 Якщо виникли проблеми

### Проблема: "Can't reach database server"
- Перевірте, що MongoDB Atlas кластер запущений
- Перевірте Network Access (дозволено ваш IP або 0.0.0.0/0)
- Перевірте правильність username та password в DATABASE_URL

### Проблема: "Authentication failed"
- Перевірте правильність пароля в DATABASE_URL
- Переконайтеся, що користувач має права доступу

### Проблема: "Invalid NEXTAUTH_SECRET"
- Переконайтеся, що ключ мінімум 32 символи
- Згенеруйте новий через `openssl rand -base64 32`
