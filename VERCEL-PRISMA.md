# 🔧 Налаштування Prisma для Vercel

## ✅ Автоматична генерація (вже налаштовано!)

У вашому `package.json` вже є правильний build скрипт:

```json
"build": "prisma generate && next build"
```

Це означає, що **Vercel автоматично виконає `prisma generate` перед build**!

## Як це працює:

1. Vercel виконує `npm install` (встановлює всі залежності, включаючи `prisma` з devDependencies)
2. Vercel виконує `npm run build`
3. Скрипт `build` спочатку виконує `prisma generate`
4. Потім виконує `next build`

## Що перевірити в Vercel Dashboard:

### 1. Build Command (має бути автоматично)

Vercel автоматично визначить Next.js проект і використає `npm run build`.

Якщо потрібно вручну налаштувати:
- Перейдіть в **Settings** → **General** → **Build & Development Settings**
- **Build Command**: `npm run build` (або залиште порожнім для автоматичного визначення)
- **Output Directory**: `.next` (або залиште порожнім)

### 2. Environment Variables

Переконайтеся, що додано всі змінні середовища:

- **Settings** → **Environment Variables**
- Додайте:
  ```
  DATABASE_URL = mongodb+srv://...
  NEXTAUTH_SECRET = ...
  NEXTAUTH_URL = https://ваш-домен.vercel.app
  ```

### 3. Root Directory (якщо проект в підпапці)

Якщо ваш репозиторій містить інші папки, встановіть:
- **Settings** → **General** → **Root Directory**: `resumeCrafto-client`

## Якщо виникають проблеми:

### Проблема: "Prisma Client is not generated"

**Рішення 1:** Переконайтеся, що `prisma` в `devDependencies`:
```json
"devDependencies": {
  "prisma": "^6.19.2"
}
```

**Рішення 2:** Додайте явний build command в Vercel:
- Settings → Build & Development Settings
- Build Command: `cd resumeCrafto-client && npm run build`

**Рішення 3:** Створіть `vercel.json` (якщо потрібно):
```json
{
  "buildCommand": "npm run build",
  "installCommand": "npm install"
}
```

### Проблема: "DATABASE_URL is not set"

**Рішення:** Додайте `DATABASE_URL` в Environment Variables в Vercel Dashboard.

## Перевірка після деплою:

1. Перейдіть в **Deployments** → виберіть останній деплой
2. Подивіться на **Build Logs**
3. Переконайтеся, що бачите:
   ```
   > prisma generate
   Prisma Client generated successfully
   > next build
   ```

## Додаткова перевірка:

Якщо хочете переконатися, що все працює:

1. Після деплою відкрийте ваш сайт
2. Спробуйте зареєструватися
3. Якщо працює - все налаштовано правильно! ✅

---

## Підсумок:

✅ **Нічого додатково налаштовувати не потрібно!**

Vercel автоматично:
- Встановить залежності (включаючи Prisma)
- Виконає `prisma generate` через build скрипт
- Збудує Next.js проект

Просто додайте Environment Variables і задеплойте! 🚀
