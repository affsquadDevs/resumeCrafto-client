# Інструкція для запуску та деплою на Vercel

## 🚀 Локальний запуск проекту

### 1. Встановлення залежностей

```bash
cd resumeCrafto-client
npm install
```

### 2. Налаштування змінних середовища

Створіть файл `.env` в корені папки `resumeCrafto-client` з наступним вмістом:

```env
# MongoDB Connection String
DATABASE_URL="mongodb+srv://<username>:<password>@cluster.mongodb.net/resume-craftor"

# NextAuth Configuration
NEXTAUTH_SECRET="your-secret-key-at-least-32-chars"
NEXTAUTH_URL="http://localhost:3000"
```

**Важливо:**
- Замініть `<username>` та `<password>` на ваші реальні дані MongoDB
- `NEXTAUTH_SECRET` - це випадковий рядок мінімум 32 символи (можна згенерувати через `openssl rand -base64 32`)
- `NEXTAUTH_URL` для локальної розробки залиште `http://localhost:3000`

### 3. Генерація Prisma Client

```bash
npx prisma generate
```

### 4. Запуск проекту в режимі розробки

```bash
npm run dev
```

Проект буде доступний за адресою: http://localhost:3000

### 5. Тестування production build (опціонально)

```bash
npm run build
npm start
```

---

## 🌐 Деплой на Vercel

### Варіант 1: Через Vercel CLI (рекомендовано)

#### 1. Встановіть Vercel CLI (якщо ще не встановлено)

```bash
npm i -g vercel
```

#### 2. Увійдіть в Vercel

```bash
vercel login
```

#### 3. Деплой проекту

```bash
cd resumeCrafto-client
vercel
```

Під час деплою:
- Виберіть опції за замовчуванням (або налаштуйте за потреби)
- Vercel автоматично визначить Next.js проект

#### 4. Додайте змінні середовища

Після першого деплою, додайте змінні середовища в Vercel Dashboard:

1. Перейдіть на https://vercel.com/dashboard
2. Виберіть ваш проект
3. Перейдіть в **Settings** → **Environment Variables**
4. Додайте наступні змінні:

```
DATABASE_URL = mongodb+srv://<username>:<password>@cluster.mongodb.net/resume-craftor
NEXTAUTH_SECRET = ваш-секретний-ключ-мінімум-32-символи
NEXTAUTH_URL = https://ваш-домен.vercel.app
```

**Важливо для NEXTAUTH_URL:**
- Для production використовуйте URL вашого Vercel проекту (наприклад: `https://resume-craftor.vercel.app`)
- Vercel автоматично надасть вам URL після деплою

#### 5. Передеплойте проект

```bash
vercel --prod
```

Або через Vercel Dashboard: натисніть **Redeploy** після додавання змінних середовища.

---

### Варіант 2: Через GitHub (автоматичний деплой)

#### 1. Завантажте код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/ваш-репозиторій.git
git push -u origin main
```

#### 2. Імпортуйте проект в Vercel

1. Перейдіть на https://vercel.com/new
2. Підключіть ваш GitHub репозиторій
3. Vercel автоматично визначить Next.js проект
4. **Важливо:** Встановіть **Root Directory** як `resumeCrafto-client` (якщо репозиторій містить інші папки)

#### 3. Додайте змінні середовища

Під час імпорту або після нього в **Settings** → **Environment Variables**:

```
DATABASE_URL = mongodb+srv://<username>:<password>@cluster.mongodb.net/resume-craftor
NEXTAUTH_SECRET = ваш-секретний-ключ-мінімум-32-символи
NEXTAUTH_URL = https://ваш-домен.vercel.app
```

#### 4. Деплой

Vercel автоматично задеплоїть проект після push в GitHub.

---

## 📝 Важливі примітки

### Prisma на Vercel

Проект автоматично запустить `prisma generate` під час build завдяки скрипту `build` в `package.json`, який виконує `prisma generate && next build`.

### MongoDB Atlas

Переконайтеся, що:
- Ваш MongoDB Atlas кластер дозволяє з'єднання з будь-яких IP (0.0.0.0/0) або додайте IP адреси Vercel
- У вас є користувач з правильними правами доступу

### Генерація NEXTAUTH_SECRET

Для генерації безпечного секретного ключа:

```bash
openssl rand -base64 32
```

Або використайте онлайн генератор: https://generate-secret.vercel.app/32

---

## ✅ Перевірка після деплою

1. Перевірте, що сайт відкривається
2. Перевірте авторизацію (якщо є)
3. Перевірте з'єднання з базою даних
4. Перевірте консоль браузера на наявність помилок

---

## 🔧 Команди для швидкого доступу

```bash
# Локальна розробка
npm run dev

# Production build
npm run build

# Запуск production сервера
npm start

# Генерація Prisma Client
npx prisma generate

# Деплой на Vercel
vercel --prod
```
