# 🔧 Виправлення помилки 500 при реєстрації

## Крок 1: Перевірте налаштування

Запустіть скрипт перевірки:

```bash
cd resumeCrafto-client
node check-setup.js
```

## Крок 2: Переконайтеся, що .env файл налаштований правильно

Файл `.env` має бути в папці `resumeCrafto-client` з наступним вмістом:

```env
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/resume-craftor?retryWrites=true&w=majority"
NEXTAUTH_SECRET="ваш-секретний-ключ-мінімум-32-символи"
NEXTAUTH_URL="http://localhost:3000"
```

**Важливо:**
- Замініть `username` та `password` на ваші реальні дані MongoDB Atlas
- `DATABASE_URL` має містити параметри `?retryWrites=true&w=majority` в кінці
- `NEXTAUTH_SECRET` - випадковий рядок мінімум 32 символи

## Крок 3: Згенеруйте Prisma Client

```bash
npx prisma generate
```

Якщо виникає помилка про DATABASE_URL, спробуйте:

```bash
# Встановіть тимчасовий DATABASE_URL для генерації
export DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/resume-craftor"
npx prisma generate
```

## Крок 4: Перевірте підключення до MongoDB

Переконайтеся, що:
1. MongoDB Atlas кластер запущений
2. IP адреса вашого комп'ютера додана в Network Access (або дозволено 0.0.0.0/0)
3. Користувач бази даних має права на читання/запис
4. Connection String правильний

## Крок 5: Перезапустіть сервер розробки

```bash
# Зупиніть сервер (Ctrl+C)
# Потім запустіть знову:
npm run dev
```

## Крок 6: Перевірте логи в консолі

Під час спроби реєстрації перевірте:
1. **Консоль терміналу** (де запущений `npm run dev`) - там будуть детальні помилки
2. **Консоль браузера** (F12 → Console) - там можуть бути помилки клієнта
3. **Network tab** (F12 → Network) - подивіться на відповідь від `/api/register`

## Типові проблеми та рішення

### Проблема: "Prisma Client is not generated"
**Рішення:**
```bash
npx prisma generate
```

### Проблема: "DATABASE_URL is not set"
**Рішення:**
- Переконайтеся, що файл `.env` існує в папці `resumeCrafto-client`
- Переконайтеся, що файл містить `DATABASE_URL=...`
- Перезапустіть сервер після створення/зміни `.env`

### Проблема: "P1001: Can't reach database server"
**Рішення:**
- Перевірте підключення до інтернету
- Перевірте, що MongoDB Atlas кластер запущений
- Перевірте Network Access в MongoDB Atlas
- Перевірте правильність Connection String

### Проблема: "P1000: Authentication failed"
**Рішення:**
- Перевірте правильність username та password в DATABASE_URL
- Переконайтеся, що користувач має права доступу до бази даних

### Проблема: "E11000 duplicate key error"
**Рішення:**
- Користувач з таким email вже існує
- Спробуйте інший email або видаліть існуючого користувача з бази

## Додаткова діагностика

Якщо проблема залишається, додайте в `.env`:

```env
NODE_ENV=development
```

І перевірте логи сервера - тепер вони покажуть детальну інформацію про помилки.

## Тестування підключення до бази даних

Створіть тестовий файл `test-db.js`:

```javascript
const { PrismaClient } = require('./src/generated/prisma');
const prisma = new PrismaClient();

async function test() {
    try {
        await prisma.$connect();
        console.log('✅ Підключення до бази даних успішне!');
        const userCount = await prisma.user.count();
        console.log(`📊 Кількість користувачів: ${userCount}`);
    } catch (error) {
        console.error('❌ Помилка підключення:', error.message);
    } finally {
        await prisma.$disconnect();
    }
}

test();
```

Запустіть:
```bash
node test-db.js
```
